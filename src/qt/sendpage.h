#ifndef SENDPAGE_H
#define SENDPAGE_H

#include <QWidget>

class SendPage : public QWidget
{
    Q_OBJECT
public:
    explicit SendPage(QWidget *parent = nullptr);

signals:

public slots:
};

#endif // SENDPAGE_H